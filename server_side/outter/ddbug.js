// global functions for debug data:

global.clog = function (msg) { return console.log(msg) };


function ddbug(msg) {
    
    function legend() {
        // used without execution to get console log
        // of these comments for TBA legend.
        // A = example etc style.
    }
    
    function slog(msg) {
        return console.log(msg);
    }
    
}